<h3>Add Magazine</h3>
<%= form_with model: @magazine do |form| %>
  <p>Title: <%= form.text_field :title %></p>

  <p>Aurthor: <%= form.select :aurthor_id, Aurthor.all.map { |aurthor| [ aurthor.name.to_s, aurthor.id ] } %></p>
  <%= form.submit %>
<% end %>
